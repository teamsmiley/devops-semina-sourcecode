name: CI-CD

on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron: "*/5 * * * *"
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        run: |
          ping 4.2.2.2
